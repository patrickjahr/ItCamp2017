import { TextToVisabilityPipe } from './text-to-visability.pipe';

describe('TextToVisabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new TextToVisabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
